# Maps

Maps make relationships visible across the repository. Appropriate formats include:

- concept and dependency maps;
- historical timelines;
- equation or model lineages;
- learning and reading paths;
- links between questions, derivations, experiments, and builds.

Every map should state its scope and update date. Link its nodes to repository entries or external sources so it remains a navigational tool rather than a decorative diagram.
